version: "3.9"

services:
  server:
    image: node:18
    container_name: scientific-hub-server
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "3000:3000"
    command: sh -c "cd server && npm install && npm start"
